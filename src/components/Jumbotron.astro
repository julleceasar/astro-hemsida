---
import Mockup from "./Mockup.astro";
---

<section
  class="relative overflow-hidden bg-linear-to-b from-white via-gray-50 to-white py-32 px-6"
>
  <div class="max-w-5xl mx-auto text-center">
    <!-- Eyebrow -->
    <p
      class="text-sm font-semibold text-blue-600 mb-6 tracking-widest uppercase"
    >
      Enkel hemsida för småföretag
    </p>

    <!-- Headline -->
    <h1
      class="text-5xl md:text-7xl font-bold text-gray-900 leading-[1.1] tracking-tight"
    >
      Få din hemsida
      <span class="block text-blue-600"> klar utan krångel. </span>
    </h1>

    <!-- Subtext -->
    <p
      class="mt-8 text-lg md:text-xl text-gray-600 max-w-2xl mx-auto leading-relaxed"
    >
      Jag bygger en snabb, modern och mobilanpassad hemsida så att du kan
      fokusera på ditt företag.
    </p>

    <!-- CTA -->
    <div
      class="mt-12 flex flex-col sm:flex-row items-center justify-center gap-4"
    >
      <!-- Primary CTA -->
      <a
        href="#contact"
        class="inline-flex items-center gap-2 bg-blue-600 text-white px-8 py-4 rounded-xl font-medium shadow-sm
         hover:shadow-md hover:scale-[1.03]
         transition duration-300 ease-out transform"
      >
        Boka ett samtal

        <!-- Arrow icon -->
        <svg
          class="w-5 h-5"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            d="M5 12h14M13 6l6 6-6 6"></path>
        </svg>
      </a>

      <!-- Secondary CTA -->
      <a
        href="#process"
        class="text-gray-700 px-8 py-4 rounded-xl font-medium border border-gray-200
         hover:border-gray-300 hover:bg-gray-50
         transition duration-300"
      >
        Så går det till
      </a>
    </div>
    <div class="relative mt-16 -mb-90">
      <div class="max-w-3xl mx-auto opacity-95 hidden sm:block">
        <Mockup />
      </div>

      <!-- Subtle background blur accent -->
      <div
        class="absolute -top-40 -right-40 w-96 h-96 bg-blue-100 rounded-full blur-3xl opacity-30"
      >
      </div>
    </div>
  </div>
</section>
